---
import SubHeading from "~/components/sub-heading/SubHeading.astro";

type Props = {
  accentText: string;
  href: string;
  level?: "h2" | "h3" | "h4" | "h5";
  text: string;
};

const { accentText, href, level = "h2", text } = Astro.props;
---

<SubHeading level={level}>
  <a
    class={`
      relative -mb-1 inline-block transform-gpu pb-1 transition-all
      after:absolute after:bottom-0 after:left-0 after:h-px after:w-full
      after:origin-bottom-right after:scale-x-0 after:bg-accent
      after:transition-transform after:duration-500
      hover:after:scale-x-100
    `}
    href={href}
  >
    {text} <span class={`text-accent`}>{accentText}</span>
  </a>
</SubHeading>
